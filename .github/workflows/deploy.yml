on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Set up Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '18'

      - name: Install dependencies
        run: npm install

      - name: Build project
        run: npm run build

      - name: Upload artifact  # Corrected upload step
        uses: actions/upload-artifact@v3
        with:
          name: dist-files
          path: ./dist/  # Ensure this path is correct!
          if-no-files-found: error # VERY IMPORTANT - Fail if no files are found

  deploy:
    runs-on: ubuntu-latest
    needs: build  # Depends on the 'build' job

    steps:
      - name: Download artifact  # Download the build output
        uses: actions/download-artifact@v3
        with:
          name: dist-files  # MUST match the name in upload-artifact
          path: ./dist/      # Download it to the same relative path

      - name: Deploy to FTP
        uses: SamKirkland/FTP-Deploy-Action@v4.3.4
        with:
          server: "31.170.166.84"
          username: "u396802645.aidoctornotes.com"
          password: "Wh0said1t#1982"
          local-dir: ./dist/   # Now, this correctly points to the downloaded artifact
          server-dir: "/public_html/"
          delete-removed: true