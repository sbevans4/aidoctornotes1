name: Build and Deploy to Hostinger (FTP)

on:
  push:
    branches:
      - main  # Trigger on pushes to 'main' (adjust if needed)
  workflow_dispatch: # Allow manual triggering

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Set up Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '18'

      - name: Install dependencies
        run: npm install

      - name: Build project
        run: npm run build

  deploy:
    runs-on: ubuntu-latest
    needs: build  # Depends on the 'build' job

    steps:
      - name: Checkout code # Checkout again in the deploy job
        uses: actions/checkout@v3

      - name: Deploy to FTP
        uses: SamKirkland/FTP-Deploy-Action@v4.3.4
        with:
          server: "31.170.166.84"
          username: "u396802645.aidoctornotes.com"
          password: "Wh0said1t#1982"
          local-dir: ./dist/
          server-dir: "/public_html/"
          delete-removed: true
